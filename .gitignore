# OS / tooling cruft
.DS_Store
.playwright-mcp/
.skill-opt/
.superpowers/

# A separate talk that happens to live alongside this one — kept local.
/gervais/

# Gallery figures are copyrighted (Thatcher & Greyling © Elsevier; Kodama ©
# Springer; the Nesta screenshot © Nesta/De Roeck). They are NOT redistributed
# here. The deck shows labelled placeholders + full provenance + the live Nesta
# link in their absence (see img/README.md). Drop your own licensed copies into
# img/ to populate the gallery locally for a talk.
img/*.png
img/*.jpg
img/*.jpeg
img/*.webp
img/*.gif
