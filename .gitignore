# OS / tooling cruft
.DS_Store
.playwright-mcp/
.skill-opt/
.superpowers/

# A separate talk that happens to live alongside this one — kept local.
/gervais/

# Gallery figures: ignore everything by default, then allow only the three we
# cleared to host (Botturi CC BY; Kodama and Nesta credited). The Thatcher &
# Greyling (1998) figure is © Elsevier and stays OUT — the deck renders it as a
# credited kintsugi gap (see the note in index.html + img/README.md).
img/*.png
img/*.jpg
img/*.jpeg
img/*.webp
img/*.gif
!img/creepyguy.png
!img/drawingtheinternet.jpg
!img/insidethesmartphone.png
